const CommonModule = (function() {
    // Preperties

    const self = this;

    // Public functions
  
    self.Authorize = function() {
       // if (!req.isAuthenticated()) {
        //     res.redirect("/login");
        // }
    };
    
    return self;
})();

module.exports = { ...CommonModule }