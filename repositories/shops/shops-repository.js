const Shop = require("../../entities/shop");

const ShopsRepository = (function () {
    // Preperties

    const self = this;

    // Private functions

    const defineSchema = () => {
       
    }

    // Public functions

    self.Init = () => {
        
    };

    return self;
})();

module.exports = { ...ShopsRepository }